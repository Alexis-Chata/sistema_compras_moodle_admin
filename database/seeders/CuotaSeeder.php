<?php

namespace Database\Seeders;

use App\Models\Cuota;
use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class CuotaSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        #crear modalidades - curso 1
            #12 cuotas
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 1'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 2'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 3'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 4'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 5'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 6'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 7'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 8'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 9'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 10'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 11'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 12'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 1;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            #6cuotas
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 1'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 2;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 2'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 2;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 3'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 2;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 4'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 2;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 5'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 2;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 6'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 2;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();


    #crear modalidades - curso 2
            #12 cuotas
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 1'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 2'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 3'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 4'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 5'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 6'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 7'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 8'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 9'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 10'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 11'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
            $n_cuota = new Cuota(); $n_cuota->name = 'cuota 12'; $n_cuota->monto = 100; $n_cuota->modalidad_id = 3;$n_cuota->fvencimiento = date('Y-m-d');$n_cuota->save();
        }
}
